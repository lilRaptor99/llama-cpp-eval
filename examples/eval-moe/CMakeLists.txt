# Placeholder so `examples/CMakeLists.txt:21 (add_subdirectory(eval-moe))` can
# resolve. This directory intentionally has no targets; sibling examples
# `eval-moe-mmlu/` and `eval-moe-popqa/` fill the role of MoE-aware
# eval tools.
